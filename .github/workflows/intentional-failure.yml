name: Intentional Failure Demo

on:
  workflow_dispatch:

jobs:
  fail-on-purpose:
    runs-on: ubuntu-latest
    steps:
      - name: Print demo message
        run: echo "This workflow is expected to fail for testing."

      - name: Fail intentionally
        run: exit 1
